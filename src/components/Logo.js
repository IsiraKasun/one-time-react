import Typography from '@mui/material/Typography';
const Logo = () => {
    return (
        <Typography variant="h2" sx={{color: '#0047AB', fontWeight: 600, textTransform: 'uppercase'}}>One Time</Typography>
    );
}

export default Logo;